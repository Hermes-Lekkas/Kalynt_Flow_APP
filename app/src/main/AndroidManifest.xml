<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".KalyntFlowApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Kalynt Flow Quick Actions Widget -->
        <receiver
            android:name=".widget.KalyntFlowQuickWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.ACTION_REFRESH_QUICK_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/kalynt_flow_quick_widget_info" />
        </receiver>

        <!-- Kalynt Flow Scrollable Priorities Widget -->
        <receiver
            android:name=".widget.KalyntFlowTasksWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.ACTION_COMPLETE_TASK" />
                <action android:name="com.example.ACTION_REFRESH_TASKS_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/kalynt_flow_tasks_widget_info" />
        </receiver>

        <!-- Scrollable ListView Service for Widget -->
        <service
            android:name=".widget.KalyntFlowWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <!-- Notification & Task Reminder Receiver -->
        <receiver
            android:name=".notifications.TaskReminderReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.example.ACTION_TRIGGER_TASK_REMINDER" />
                <action android:name="com.example.ACTION_COMPLETE_TASK_NOTIFICATION" />
                <action android:name="com.example.ACTION_SNOOZE_TASK" />
                <action android:name="com.example.ACTION_DAILY_BRIEFING" />
                <action android:name="com.example.ACTION_BACKGROUND_SYNC" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
